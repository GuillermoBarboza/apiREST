<!DOCTYPE html>
<html lang="en">
<%- include("partials/head") %>

<body>
  <%- include("partials/navbar") %>

  <div class="container">
    <% if(userSession._id != "voidUsername") { %>
    <h1 class="titleUserHome">Bienvenido a Twita</h1>

    <div class="row">
      <div class="col-12">
        <h3>Twiteale algo al mundo</h3>
        <form class="form-group" action="/twitealo" method="POST">
          <input type="hidden" name="author" value=<%- userSession._id %> />
          <input type="hidden" name="dateOfCreation" value=<%-Date.now()%> />
          <textarea name="body" rows="4"> </textarea>
          <div><button type="submit">Twit!</button></div>
        </form>
       
        <form action="/follow" method="POST">

          <input type="hidden" name="author" value=<%- twit.author._id %> />
          <button type="submit">Follow</button>
        </form>
      



      </div>
      <!-- Twit -->
      <div class="col-12">
        <p>
          <%- twit.body %>
        </p>
        <div>
          <p> <%- twit.dateOfCreation %></p>
          <p> <%- twit.likes.length %></p>
          <% if (twit.likes.indexOf(user._id) != -1) { %>
          <form action="/like" method="POST">

            <input type="hidden" name="author" value=<%- twit._id %> />
            <button type="submit">Dislike</button>

          
            
          </form>
          <% } else { %>
          <form action="/like" method="POST">

          <input type="hidden" name="author" value=<%-twit._id %> />
            <button type="submit">Like!</button>
          </form>
          <% } %>
          


        </div>
        <!-- Twit -->
        <div class="col-12">
          <p>
            <%- twit.body %>
          </p>
          <div>
            <p> <%- twit.dateOfCreation %></p>
            <p> <%- twit.likes.length %></p>
            <% if (twit.likes.indexOf(userSession._id) != -1) { %>
            <form action="/like" method="POST">

              <input type="hidden" name="author" value=<%- twit._id %> />
              <button type="submit">Dislike</button>

            </form>
            <% } else { %>
            <form action="/like" method="POST">

              <input type="hidden" name="author" value=<%-twit._id %> />
              <button type="submit">Like!</button>
            </form>
            <% } %>
          </div>

        
        </div>

      <% } else { %> 
      <h2 class="titleHome">Ingresar a Twita</h3>
          <h6 class="text-center">Únete al twita ya mismo</h5>
            <div class="d-flex justify-content-center">
              <a class="btn btn-signUp btn-lg" href="http://localhost:3000/registro">Regístrate aquí</a>
              <a class="btn btn-logIn-home btn-lg" href="http://localhost:3000/login">Iniciar sesión</a>
              
            </div>
      </div>
      <% } %>
    </div>

  
</body>

</html>